# Active Next.js web app — no environment variables are required.
# All analytics are computed from the committed CSVs in data/cleaned.
NEXT_PUBLIC_APP_NAME=Nazava Analytics

# Optional FastAPI backend (docker-compose)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000
FORECAST_DAYS=30
CONFIDENCE_INTERVAL=0.95
